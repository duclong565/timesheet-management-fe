import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: Port is typically set via environment variable or CLI
  // This is just for reference - prefer .env.local or package.json scripts
};

export default nextConfig;
